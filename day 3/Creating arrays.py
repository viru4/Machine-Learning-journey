import numpy as np
a= np.zeros(5)
# creates an array filled with 0
b= np.ones(5)
# creates an array filled with 1
c= np.arange(0,10,2)
# (start, stop, step) like range() method but return numpy array
d= np.linspace(0,1,5)
# (start, end, total numbers needed) creates equal spacing between two numbers
e= np.random.randint(1,10,5)
# (low, high, how many values) generates random integers